# Implementation Map: Concept to Code Traceability

> **Learning Status**: NOT STUDIED / PENDING

This document provides complete, line-by-line architectural traceability linking every conceptual requirement to its source implementation, pipeline stage, and output artifact.

---

## Traceability Matrix

| # | Concept / Requirement | Source Table / Batch | File & Function | Output Artifact | Architectural Why |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | **Deterministic Initial State** | `accounts`, `subscriptions`, `invoices`, `payments` | [initial_state.py](../src/generation/initial_state.py)<br>`generate_initial_state()` | `data/sample/snapshot/*.jsonl`<br>`snapshot_metadata.json` | Guarantees test reproducibility: fixed seed + scale yields identical records and sequence counters. |
| **2** | **CDC Batch Generation & Envelope** | All 4 domain entities | [cdc_generator.py](../src/generation/cdc_generator.py)<br>`generate_batch()` | `data/generated/cdc/batch_id=XXXXXX/*.jsonl`<br>`manifest.json` | Simulates realistic OLTP mutations (inserts, updates, deletes) with manifest metadata for ingestion validation. |
| **3** | **Deterministic Event ID** | All change events | [cdc_schema.py](../src/schemas/cdc_schema.py)<br>`generate_event_id()` | `CDCEvent.event_id` | SHA-256 hash of immutable change coordinates enables exact deduplication across retries. |
| **4** | **Source Sequence (Simulated LSN)** | All change events | [cdc_generator.py](../src/generation/cdc_generator.py)<br>`_next_seq()` | `CDCEvent.source_sequence` | Simulates WAL/binlog position, providing absolute ground truth for mutation ordering. |
| **5** | **Explicit PySpark Schema** | Change event ingestion | [cdc_schema.py](../src/schemas/cdc_schema.py)<br>`CDC_SPARK_SCHEMA` | DataFrame with fixed `StructType` | Prevents runtime schema inference overhead, type coercion bugs, and corrupted data ingestion. |
| **6** | **Batch Discovery & Ordering** | CDC batch directories | [discovery.py](../src/cdc/discovery.py)<br>`discover_batches()` | `List[DiscoveredBatch]` sorted numerically | Ensures batch 9 is processed before batch 10 and rejects malformed directories. |
| **7** | **Schema Validation & Quarantine** | All ingested events | [validation.py](../src/cdc/validation.py)<br>`validate_cdc_dataframe()` | `output/valid_events/`<br>`output/quarantine/` | Invalid/corrupt records are safely quarantined with exact rejection reasons rather than silently dropped. |
| **8** | **Deterministic Deduplication** | Events with duplicate IDs | [deduplication.py](../src/cdc/deduplication.py)<br>`deduplicate_cdc_events()` | `deduped_valid_df`<br>`duplicates_df` | `ROW_NUMBER()` over `event_id` ordered by ingestion timestamp and canonical hash provides stable tie-breaking. |
| **9** | **Source-Sequence Ordering** | Valid change stream | [ordering.py](../src/cdc/ordering.py)<br>`order_cdc_events()` | Ordered DataFrame by `(table, key, seq)` | Solves out-of-order physical arrival by restoring true logical state progression. |
| **10** | **High-Water Mark & Checkpoints** | Incremental progress | [checkpoint.py](../src/state/checkpoint.py)<br>`CheckpointManager` | `state/checkpoints/checkpoint_state.json` | Atomic rename (`os.replace`) on unified state file guarantees all-or-nothing batch commitment across tables. |
| **11** | **Regression Protection** | Sequence verification | [checkpoint.py](../src/state/checkpoint.py)<br>`advance_high_water_mark()` | `CheckpointRegressionError` | Prevents backwards time travel or accidental state rewind. |
| **12** | **Late-Event Isolation** | Events with `seq <= HWM` | [processor.py](../src/cdc/processor.py)<br>`process_batch()` | `output/late_events/` | Late-arriving events are preserved for Module 3 reconciliation rather than dropped. |
| **13** | **Metrics Reconciliation** | Batch summary | [processor.py](../src/cdc/processor.py)<br>`process_batch()` | `output/metrics/batch_id=XXXXXX/metrics.json` | Verifies invariant: `raw == valid_unique + duplicates + invalid`. |
| **14** | **Diagnostic Analytics** | Processed outputs | [analysis.py](../src/cdc/analysis.py)<br>`CDCAnalyzer` | Temporary views (`v_cdc_valid`, `v_cdc_quarantine`, `v_cdc_late`) | Diagnostic aggregations for operations, sequences, update frequencies, and quarantine reasons. |
| **15** | **Delta Spark Factory** | SparkSession configuration | [spark.py](../src/utils/spark.py)<br>`get_spark_session()` | Delta-enabled PySpark local session | Configures `io.delta.sql.DeltaSparkSessionExtension` and `DeltaCatalog` via `delta-spark 3.2.1`. |
| **16** | **Target Schemas & Decimal Types** | Target current-state tables | [schemas.py](../src/delta/schemas.py) | Explicit `StructType` per entity | Enforces `DecimalType(12, 2)` for monetary attributes (`monthly_amount`, `amount`) and lineage metadata. |
| **17** | **Payload Parsing (Catalyst)** | Serialized JSON payloads | [payload_parser.py](../src/delta/payload_parser.py)<br>`parse_cdc_payloads()` | Typed DataFrames per table | Uses built-in Spark Catalyst `from_json()` with explicit schemas under full after-image contract. |
| **18** | **Snapshot to Delta Current State** | Initial OLTP JSONL | [current_state.py](../src/delta/current_state.py)<br>`initialize_delta_current_state()` | `delta/current/<table_name>/` | Materializes snapshot rows with `_is_deleted=false` and baseline lineage metadata. |
| **19** | **Active Table Views** | Current-state queries | [current_state.py](../src/delta/current_state.py)<br>`load_active_table()` | DataFrame filtered by `_is_deleted=false` | Excludes soft-deleted tombstones for operational and analytical consumption. |
| **20** | **Within-Batch Event Collapsing** | Multiple updates to same key | [merge.py](../src/delta/merge.py)<br>`collapse_in_batch_mutations()` | `winners_df`<br>`superseded_df` | `ROW_NUMBER() OVER (PARTITION BY key ORDER BY seq DESC)` avoids ambiguous multi-match Delta MERGE errors. |
| **21** | **Delta MERGE Mutation Engine** | Collapsed candidate winners | [merge.py](../src/delta/merge.py)<br>`apply_delta_merge()` | Updated Delta tables | Conditional `whenMatchedUpdate` and `whenNotMatchedInsert` enforcing soft delete and sequence progression. |
| **22** | **Source-Sequence Conflict Guard** | Out-of-order/stale changes | [merge.py](../src/delta/merge.py)<br>`apply_delta_merge()` | Unmodified target row | `source.source_sequence > target._last_source_sequence` prevents stale events from overwriting newer state. |
| **23** | **Soft-Delete Tombstone Semantics** | DELETE operations | [merge.py](../src/delta/merge.py)<br>`apply_delta_merge()` | Target row with `_is_deleted=true` | Preserves sequence history and business key on delete, enabling crash recovery and conflict protection. |
| **24** | **Protective Tombstone for Unknown Keys** | DELETE on unseen key | [merge.py](../src/delta/merge.py)<br>`apply_delta_merge()` | Tombstone row with NULL attributes | Records delete sequence memory so any older out-of-order INSERT arriving later is safely rejected. |
| **25** | **Re-Insert After Delete** | Higher-sequence INSERT | [merge.py](../src/delta/merge.py)<br>`apply_delta_merge()` | Target row reactivated (`_is_deleted=false`) | Overwrites tombstone with new after-image when incoming insert sequence exceeds delete sequence. |
| **26** | **Applied Event Ledger** | All valid processed events | [audit.py](../src/delta/audit.py)<br>`upsert_applied_events()` | `delta/audit/applied_events/` | Idempotent Delta MERGE on `event_id` records every mutation outcome (`APPLIED_INSERT`, `SUPERSEDED_WITHIN_BATCH`, etc.). |
| **27** | **Batch Apply Metrics** | Apply batch execution | [metrics.py](../src/delta/metrics.py)<br>`record_batch_apply_metric()` | `delta/audit/batch_apply_metrics/` | Persists counts for inputs, superseded, applied inserts/updates/deletes, stale, active, and deleted rows. |
| **28** | **Target/Ledger Crash Recovery** | Interrupted batch retries | [delta_cdc_apply.py](../src/pipelines/delta_cdc_apply.py)<br>[merge.py](../src/delta/merge.py) | Repaired audit ledger | Pre-merge target inspection detects prior mutation, prevents double-mutation, and records `RECOVERED_AFTER_PARTIAL_COMMIT`. |
| **29** | **Batch-Atomic Checkpoint Advance** | End of batch pipeline | [checkpoint.py](../src/state/checkpoint.py)<br>`commit_batch()` | `state/checkpoints/checkpoint_state.json` | Advances checkpoint high-water marks across all 4 tables simultaneously only after all Delta writes succeed. |
| **30** | **Current-State Invariant Reconciliation** | Mutated Delta tables | [reconciliation.py](../src/delta/reconciliation.py)<br>`reconcile_current_state()` | `CurrentStateReconciliationResult` | Validates: 1 row per PK, no null PKs, sequences > 0, active + deleted = total, Decimal money, and unique event IDs. |
| **31** | **Canonical CDC Event Store** | All valid deduplicated events | [event_store.py](../src/events/event_store.py)<br>`upsert_canonical_events()` | `delta/events/cdc_event_store/` | Durable historical event store (14 fields) capturing all mutations prior to current-state collapsing. |
| **32** | **Event Store Idempotency & Conflict Detection** | Incoming canonical events | [event_store.py](../src/events/event_store.py)<br>`upsert_canonical_events()` | `EventIdentityConflictError` | MERGE on `event_id`. Re-delivery updates safe audit metadata; conflicting payload raises explicit error. |
| **33** | **Late-Event Replay Queue** | Unseen late CDC mutations | [queue.py](../src/replay/queue.py)<br>`upsert_replay_queue_entries()` | `delta/replay/late_event_queue/` | Manages late changes pending historical reconstruction (`PENDING`, `APPLIED`, `NOOP`, `CONFLICT`, `FAILED`). |
| **34** | **Operational SCD2 History** | Subscriptions entity history | [subscriptions_scd2.py](../src/history/subscriptions_scd2.py)<br>`initialize_subscription_history()` | `delta/history/subscriptions_history/` | Source-system state history ordered by `source_sequence` interval `[valid_from_sequence, valid_to_sequence)`. |
| **35** | **Deterministic History Version ID** | Historical state versions | [subscriptions_scd2.py](../src/history/subscriptions_scd2.py)<br>`generate_history_version_id()` | `history_version_id` | SHA-256 hash of `subscription_id + valid_from_sequence + source_event_id` ensures durable version identity. |
| **36** | **Key-Scoped Timeline Rebuild** | Targeted subscription key | [history_rebuilder.py](../src/replay/history_rebuilder.py)<br>`rebuild_subscription_history_for_key()` | Corrected SCD2 history partition | Loads snapshot baseline + full event store for key, checks collisions, rebuilds timeline, and verifies 11 invariants. |
| **37** | **Replay Current-State Drift Verification** | History vs current Delta state | [history_rebuilder.py](../src/replay/history_rebuilder.py)<br>`verify_current_state_alignment()` | `CurrentStateDriftError` | Verifies highest history sequence and business attributes match current-state without silent mutation. |
| **38** | **Replay Audit Ledger & Crash Recovery** | Historical replay execution | [audit.py](../src/replay/audit.py)<br>`record_replay_audit()` | `delta/replay/replay_audit/` | Tracks before/after versions and sequences. Crash recovery detects already-reconstructed history on retry. |
| **39** | **Replay Checkpoint Isolation** | Global pipeline checkpoints | [processor.py](../src/replay/processor.py)<br>`process_replay_queue()` | `checkpoint_state.json` unchanged | Enforces that late-event replay (a correction lane) never rewinds or mutates global ingestion high-water marks. |
| **40** | **Schema Version Registry** | Payload schemas V1 & V2 | [registry.py](../src/schema_evolution/registry.py)<br>`PAYLOAD_SCHEMA_REGISTRY` | Version-aware PySpark schemas | Manages explicit payload schemas by `(source_table, schema_version)` while keeping envelope schema stable. |
| **41** | **Controlled Schema Evolution** | Evolving Delta tables | [migrations.py](../src/schema_evolution/migrations.py)<br>`migrate_subscriptions_to_v2()` | Subscriptions table with V2 columns | Explicitly adds `billing_cycle` and `currency` via controlled migration without globally enabling unsafe autoMerge. |
| **42** | **Unsupported Schema Quarantine** | Ingested events with bad version | [validation.py](../src/cdc/validation.py)<br>`validate_cdc_dataframe()` | `output/quarantine/` | Quarantines events carrying unknown schema versions under `UNSUPPORTED_SCHEMA_VERSION`. |

